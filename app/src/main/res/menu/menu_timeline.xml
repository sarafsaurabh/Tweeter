<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.codepath.apps.tweeter.activity.TimelineActivity">

    <item
        android:id="@+id/miActionProgress"
        android:title="Loading..."
        android:visible="false"
        app:showAsAction="ifRoom"
        app:actionLayout="@layout/action_view_progress" />

    <item
        android:id="@+id/miProfile"
        android:icon="@mipmap/ic_profile"
        app:showAsAction="ifRoom"
        android:onClick="onProfileView"
        android:title="@string/profile">
    </item>

    <item
        android:id="@+id/miCompose"
        android:icon="@mipmap/ic_quill"
        app:showAsAction="always"
        android:onClick="onComposeAction"
        android:title="@string/compose">
    </item>
</menu>
